# Project Rules

- When performing Visual Verification or navigating using Chrome DevTools Protocol (CDP), always open a new tab rather than using or navigating an existing active tab, to avoid disrupting the user's active browser session.
